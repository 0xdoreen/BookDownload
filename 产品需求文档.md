# 自动电子书下载工具 产品需求文档（PRD）

- 文档版本：v0.2（需求确认稿）
- 更新日期：2026-09-13
- 产品代号：BookDownload（暂定）
- 文档状态：已根据 Doreen 第一轮 Review 更新

## 1. 产品概述

### 1.1 背景

用户拥有一份需要下载的图书清单，希望工具能够解析书单、在用户已登录且有权访问的电子书渠道中检索候选资源、判断候选资源是否与目标图书一致，并在用户授权后完成下载、归档和文件重命名。

当前指定下载渠道为 Z-Library 网站，初始地址为 `https://zlib.bz/`。首版仅支持网页方式，不支持 PC 客户端。工具不提供账号、不绕过登录、验证码、下载额度、付费、DRM、地区限制或网站的其他访问控制。

### 1.2 产品目标

1. 将半结构化书单自动转换为可处理的标准图书条目。
2. 尽可能准确地找到用户真正想要的书，减少同名书、错作者、节选版和错误卷次造成的误下载。
3. 在多个匹配资源中，按用户偏好选择更合适的版本：先选择较新的正式完整版本，再在同等版本中优先 PDF 格式。
4. 自动记录下载结果并规范文件名，方便后续检索和阅读。
5. 对不确定、可能错误或需要用户操作的情况提供清晰的人工确认入口。

### 1.3 非目标（首版不包含）

1. 不破解 DRM，不规避验证码、限流、会员权限或其他安全机制。
2. 不自动注册账号、购买会员或处理支付。
3. 不保证每一本书都能找到或下载。
4. 不在未经用户确认的情况下自动改写原始书单。
5. 不进行电子书内容翻译、OCR、格式转换或内容修复。
6. 不向第三方重新分发已下载文件。
7. 首版不接入 Z-Library PC 客户端。

## 2. 目标用户与典型场景

### 2.1 目标用户

- 持有合法访问权限，希望批量整理个人电子书的用户。
- 书单中可能同时包含中文书名、英文书名、作者、系列名、卷次、AR、蓝思值及简介的用户。

### 2.2 典型使用场景

用户提供类似 `示例1.txt` 的书单并选择保存目录。工具解析出待处理图书，允许用户修正解析结果；用户登录下载渠道后，工具逐本检索并评估候选项。高置信度结果可以按用户设置自动下载，低置信度结果交由用户确认。下载完成后，工具清理站点附加后缀、补充英文书的阅读难度信息，并生成结果报告。

## 3. 核心使用流程

1. 用户选择或拖入书单文件。
2. 工具解析书单并展示“待下载图书表”。
3. 用户检查、编辑、合并或删除误解析条目，然后确认清单。
4. 用户选择下载文件保存目录。
5. 工具打开可见浏览器，由用户自行登录 Z-Library；登录成功后，后续检索和下载切换到无头浏览器执行。
6. 工具逐本检索候选资源并计算匹配度。
7. 工具按规则选择候选项：
   - 高置信度：根据用户设置自动下载或下载前确认；
   - 中置信度：展示前若干候选项，由用户选择；
   - 低置信度或无结果：标记为未找到，不下载。
8. 工具监测下载结果，校验文件是否完整落盘。
9. 工具按命名规则重命名并处理重名文件。
10. 工具生成任务报告，列出成功、跳过、失败、待确认及其原因。

## 4. 功能需求

### FR-01 书单导入

#### 输入格式

首版及当前规划仅支持 UTF-8 编码的 `.txt` 文件，并能识别其中的 Markdown 风格排版。不规划 `.csv`、`.xlsx` 或手工粘贴输入。

#### 解析字段

每个图书条目使用以下标准字段：

| 字段 | 必填 | 说明 |
|---|---:|---|
| 原始文本 | 是 | 保存可追溯的原始条目 |
| 主书名 | 是 | 原文书名，优先用于搜索 |
| 中文译名 | 否 | 如《屋顶上的索菲》 |
| 作者 | 否 | 用于消除同名书歧义 |
| 系列名 | 否 | 例如 Crookhaven |
| 卷次/子标题 | 否 | 区分系列中的具体书目 |
| 语言 | 否 | 未提供时由工具推断 |
| ISBN | 否 | 若提供，则作为最高优先级身份标识 |
| 出版年份/版次 | 否 | 若提供，用于版本筛选 |
| AR 值 | 否 | 区分正式数据与估算值 |
| 蓝思值 | 否 | 区分正式数据与建议值 |
| 简介/备注 | 否 | 仅作辅助，不应被解析为书名 |

#### 解析要求

- 能识别标题、分类标题、编号条目、Markdown 加粗、括号中的作者/译名等结构。
- 能识别“一个系列条目下包含多本子书”的情况，并拆分成独立待下载条目。
- 不把分类标题、简介、AR/蓝思字段或网页残留文本当成书名。
- 解析后必须提供预览；用户确认前不得开始下载。
- 对缺少作者、书名疑似残缺或结构不明确的条目显示警告。

### FR-02 保存目录设置

- 用户必须能够选择本地保存目录。
- 工具开始任务前验证目录存在、可写且有合理的可用空间。
- 所有下载文件直接保存到用户所选目录，不按作者、系列或语言建立子目录。
- 工具不得覆盖已有文件，除非用户明确选择“覆盖”。
- 保存目录和最近使用设置可本地记忆，但不得上传。

### FR-03 登录与渠道接入

#### 浏览器模式（首版唯一接入方式）

- 打开渠道登录页，由用户亲自完成账号、密码、验证码及多因素认证。
- 登录阶段使用可见浏览器；确认登录成功后，检索、匹配和下载阶段默认切换为无头浏览器。
- 工具不得读取、保存或记录用户密码。
- 登录失效时暂停任务并提示用户重新登录。
- 页面结构变化导致无法继续时停止自动操作并给出具体错误。
- 默认按普通账号每 24 小时最多下载 10 本进行批次控制；实际可用额度以网站显示或返回结果为准。
- 达到当期下载额度后，保存进度并暂停任务，向用户显示已完成数量、剩余数量和可继续操作的提示；不得通过切换账号、镜像或其他方式规避额度。
- 默认在额度耗尽后的 24 小时安排自动续跑；未完成队列必须持久化，程序重启后仍可恢复。若续跑时登录已失效，则将任务置为“等待重新登录”，用户登录后继续。
- 网站地址必须作为可配置项保存，不硬编码到渠道逻辑中。
- 当地址无法访问、发生重定向异常或页面特征不匹配时，暂停任务并提示用户提供新的有效网站地址；工具不得自行搜索或切换镜像站。

#### 合规约束

- 仅下载用户有权获取的内容。
- 遵守渠道服务条款、robots/自动化政策、频率限制和账号下载额度。
- 不自动切换镜像站、不伪造身份、不绕过访问控制。

### FR-04 图书检索

工具应按信息完整度构造检索词，推荐顺序为：

1. ISBN；
2. 精确书名 + 作者；
3. 精确书名 + 系列名/卷次；
4. 精确书名；
5. 去除标点、冠词或译名后的宽松书名。

国外正规出版社出版的现代图书通常有 ISBN，因此只要书单或候选页面提供 ISBN，就将其作为最强身份标识。但 ISBN 不是必填项：1970 年前出版物、部分自出版物、特定版本或站点元数据缺失时可能没有可用 ISBN。缺少 ISBN 时，继续使用书名、作者、系列、卷次、语言和出版信息交叉匹配。

每本书应保存检索时间、检索词、候选数量及候选摘要，以便排查错误。

### FR-05 AI 候选匹配与版本选择

#### 候选匹配

工具需综合以下信息判断候选资源是否为目标图书：

| 维度 | 建议权重 | 说明 |
|---|---:|---|
| ISBN | 决定性 | 完全一致时可直接确认身份 |
| 主书名/子标题 | 35% | 允许标点、大小写和轻微拼写差异 |
| 作者 | 25% | 作者不一致通常应显著降分 |
| 系列与卷次 | 15% | 防止下载错卷或合集 |
| 语言 | 10% | 默认匹配书单原文语言 |
| 出版社/出版年份 | 5% | 用于辅助版本判断 |
| 文件信息 | 10% | 格式、页数、大小、完整性提示 |

以下情况必须降低置信度或排除：摘要版、试读版、节选、学习指南、教师用书、练习册、错误卷次、明显不符的作者或语言、异常小文件、缺页或损坏标记。

#### 置信度与人工确认

- 高置信度（建议 ≥ 90）：可进入自动下载队列；用户可配置为仍需确认。
- 中置信度（建议 70–89）：必须向用户展示候选项及判断依据。
- 低置信度（建议 < 70）：不得自动下载，标记为未确认。
- 同分、同名异书、作者缺失、系列卷次冲突时，即使总分较高也应要求确认。

阈值和权重属于初始方案，应通过真实书单测试后调整。

#### 版本优先级

在确认是同一本书后，默认按以下顺序选择：

1. 完整、可正常下载且无损坏迹象；
2. 用户指定语言；
3. 最新正式版本/较新出版年份；
4. 同一版本存在多种格式时优先 PDF；
5. 元数据更完整、页面数和文件大小更合理的版本。

“最新版本”指可验证出版年份或版次较新的正式完整版本，而不是上传时间最新，也不把正文未变化的普通重印视为新版。当较新的 EPUB 与较旧的 PDF 冲突时，默认选择较新的 EPUB；只有属于同一版本时才优先 PDF。扫描版 PDF 与文本型 PDF 应显示区别；若质量明显较差，不应仅因其是 PDF 就自动胜出。

### FR-06 下载执行

- 支持顺序下载；默认采用保守频率，避免触发渠道限流。
- 高置信度候选默认自动下载，不要求逐本点击确认。
- 每次下载前再次检查登录状态、下载额度及目标目录。
- 能识别下载开始、进行中、成功、被拒绝、超时和失败。
- 对网络临时错误允许有限次数重试，默认最多 2 次，并采用递增等待。
- 对验证码、额度耗尽、权限不足等情况不得自动重试或规避，应暂停并提示用户。
- 下载完成的判定至少包括：临时下载后缀消失、文件大小稳定、文件可读取。
- 可选进行 PDF 文件头及页数基础校验；损坏文件应隔离并标记失败。
- 支持暂停、继续和取消剩余任务；取消不得删除已成功下载的文件。

### FR-07 去重与断点续跑

- 任务开始前仅扫描用户本次指定的保存目录及本工具的历史记录，判断目标书是否可能已下载；首版不扫描电脑上的其他电子书目录。
- 优先使用 ISBN 或内容哈希去重；缺少 ISBN 时使用规范化书名 + 作者 + 语言辅助判断。
- 对疑似已有但无法确定的文件要求用户确认，不直接覆盖或重复下载。
- 保存任务进度；程序意外退出后可继续未完成项目。

### FR-08 文件重命名

#### 默认命名模板

- 英文书：`系列名 - 作者 - 书名 - 卷次 - Lexile - AR.扩展名`
- 非英文书：`系列名 - 作者 - 书名 - 卷次.扩展名`

文件名不包含出版社和 ISBN。系列名、作者、卷次、蓝思值或 AR 值未知时，省略对应片段并自动整理分隔符，不使用 `Unknown` 占位。没有系列信息时，英文书示例为：`Katherine Rundell - The Explorer - [Lexile 740L] - [AR 5.4].pdf`。用户后续可自定义模板，但默认字段顺序保持为“系列名—作者—书名—卷次—蓝思—AR”。

#### 清理规则

- 去除 Z-Library 附加在文件名末尾的来源标记，包括但不限于：`(z-library.sk, 1lib.sk, z-lib.sk)`。
- 清除扩展名前重复的站点域名、无意义编号和多余空格，但保留书名、作者、版次等有效信息。
- 仅删除能够明确识别的站点附加文本，不应误删原书名中的括号内容。
- 替换 Windows 不允许的文件名字符：`< > : \ / | ? * \"`。
- 文件名过长时按“保留书名和作者优先”的规则安全截断。
- 同名冲突默认追加 ` (2)`、` (3)`，不得静默覆盖。

#### AR 与蓝思值规则

- 书单已提供正式 AR/蓝思数据时，优先采用书单数据。
- 若书单标注为“估算”，文件名中也应保留估算属性，例如 `[Lexile ~750L] [AR ~5.1]`。
- 若书单没有数据，工具应先查询已配置的外部可信数据源；仍缺失时可重新估算“建议值”，但必须标明为估算，不得表现为官方数据。
- 建议值应基于可信元数据或文本特征；无法可靠判断时不写入文件名。
- 建议同时把数值、来源、是否估算和置信度写入报告，避免文件名承载全部信息。

### FR-09 任务报告

每次任务生成一份本地报告，至少包含：

- 总条目数，以及成功、跳过、失败、待确认、未找到的数量；
- 每本书的原始条目、最终识别结果、候选匹配分及关键依据；
- 下载文件原名、重命名后名称和保存位置；
- AR/蓝思值、数据来源及“正式/估算”标识；
- 失败原因、重试次数及建议处理动作；
- 任务开始/结束时间。

建议首版输出 CSV 和 JSON 两种格式，界面中提供简要汇总。

### FR-10 设置

首版提供以下设置；括号中为默认值：

- 高置信度结果是否自动下载（是）；
- 文件格式优先顺序（同版本优先 PDF）；
- PDF 与最新版本冲突时的优先策略（版本优先，即选择较新的 EPUB）；
- 语言偏好；
- 是否将 AR/蓝思估算值写入文件名（是，并带 `~` 标记）；
- 下载重试次数；
- 同名文件处理方式；
- 文件名模板。

### FR-11 下载列表管理与定时续跑

- 下载列表持久化保存在本机，程序退出或电脑重启后不得丢失。
- 用户可以查看全部项目，并按状态筛选待下载、下载中、待确认、暂停、失败和已完成项目。
- 用户可以暂停、继续、取消、删除未开始项目，以及将失败项目重新加入队列；删除队列项目不得删除已经下载的文件。
- 达到账号当期额度时，当前未完成项目进入“额度等待”状态，并记录下一次尝试时间，默认是检测到额度用尽后的 24 小时。
- 常驻 worker 到达下一次尝试时间后自动继续处理队列，不要求用户重新导入书单。
- 若程序在计划时间没有运行，则下次启动 worker 时立即处理已经到期的任务。
- 自动续跑不得绕过登录：会话失效时状态变为“等待重新登录”，由用户完成可见浏览器登录后恢复。
- 首版队列按导入顺序执行；后续可以扩展手工调整优先级。

## 5. 页面与交互需求

首版采用“命令行 + 简单确认界面”，至少包含以下交互：

1. **新建任务**：选择书单、保存目录和网站地址。
2. **解析确认页**：以表格展示图书条目，支持编辑、删除、合并和拆分。
3. **候选确认页**：并排展示目标信息、候选资源、匹配分和推荐理由。
4. **任务执行页**：展示当前图书、整体进度、下载状态、暂停/继续/取消。
5. **结果页**：展示统计、失败原因，并可打开保存目录或导出报告。
6. **设置页**：配置格式、版本、命名、确认和重试策略。

所有 AI 判断都应提供简短、可读的依据，例如：“书名与作者完全一致；语言一致；2024 年 PDF；页数合理”。不能只显示一个无法解释的分数。

## 6. 状态模型

每个图书条目只能处于以下一种主状态：

`待解析 → 待确认 → 检索中 → 候选待确认/待下载 → 下载中 → 校验中 → 已完成`

异常终态包括：`已跳过`、`未找到`、`登录受阻`、`额度受限`、`下载失败`、`文件损坏`、`用户取消`。

状态变化和错误原因必须写入任务记录，以支持断点续跑。

## 7. 非功能需求

### 7.1 安全与隐私

- 密码、Cookie、令牌等敏感信息不得写入日志或报告。
- 首版数据默认只保存在本机。
- 日志中的 URL 若包含令牌或用户标识，必须脱敏。
- AI 若使用云端模型处理书单，应明确提示会发送哪些字段，并提供关闭或本地处理选项。

### 7.2 可靠性

- 单本失败不得导致整个批次丢失。
- 重启后能够从已保存状态恢复。
- 重命名采用安全的两阶段流程，失败时保留原文件，不造成文件丢失。

### 7.3 性能

- 解析 1,000 条纯文本书目目标耗时不超过 30 秒（不含云端模型响应）。
- 界面执行下载时保持可响应。
- 检索与下载速度服从渠道限制，不以高并发为首版目标。

### 7.4 可维护性

- 将“书单解析”“渠道适配”“匹配评分”“下载管理”“元数据补充”“文件命名”设计成独立模块。
- 渠道页面选择器和匹配权重应可配置，便于网站变化后修复。
- 关键流程有结构化日志，但日志不得包含账号凭证。

## 8. MVP 范围建议

### P0：首个可用版本

- 导入并解析 `.txt`/Markdown 书单；
- 解析结果人工确认；
- 设置保存目录；
- 可见浏览器中由用户自行登录，随后切换无头浏览器；
- 网站地址可配置，失效时要求用户提供新地址；
- 普通账号按每 24 小时 10 本进行批次控制，并根据网站实际额度暂停；
- 持久化下载列表、列表管理命令和 24 小时额度等待后的自动续跑 worker；
- 单渠道检索与候选采集；
- 基于书名、作者、系列、语言和文件信息的候选评分；
- 中低置信度人工确认；
- 顺序下载、基础重试与状态展示；
- 下载完成检测、去重、重命名；
- 使用书单已有 AR/蓝思值；缺失时查询外部数据源，必要时生成有明确标识的估算值；
- CSV/JSON 任务报告和断点续跑。

### P1：增强版本

- 自动查询公开、可信的 AR/蓝思元数据来源；
- 在缺少正式数据时计算并标注建议值；
- PDF 文本型/扫描型与基础质量识别；
- 自定义文件名模板；
- 进一步提高外部阅读难度数据源的覆盖率和估算质量。

### P2：后续方向

- 多个合法内容渠道适配；
- 本地书库索引、封面与元数据管理；
- 内容质量检测、OCR 或格式转换（需作为独立能力评估）。

## 9. MVP 验收标准

1. 对 `示例1.txt`，工具能生成可编辑的独立图书列表；分类标题、简介和 AR/蓝思行不会被误判为独立书目。
2. `The School for Thieves` 下的多本子书能够被识别并拆分，或明确标记给用户确认。
3. 未经用户确认解析结果与保存目录，工具不会开始检索和下载。
4. 对存在多个候选项的图书，界面能显示书名、作者、语言、格式、年份、页数/大小（若渠道提供）、匹配分及推荐理由。
5. 作者或卷次冲突的候选项不会被自动下载。
6. 登录失效、验证码、额度不足或权限受限时，任务会暂停并提示，不会尝试绕过。
7. 下载成功后，来源后缀被正确清理；英文书按设置添加 AR/蓝思字段。
8. 若 AR/蓝思为估算值，文件名和报告中均有明确的估算标识。
9. 已有同名文件不会被静默覆盖；任务中断后可以继续。
10. 任务结束后可获得完整报告，并能追溯每本书为何成功、跳过或失败。
11. 用户登录后，后续操作能够在无头浏览器中执行；地址失效时会要求用户输入新地址。
12. 高置信度候选默认自动下载；达到账号下载额度后保存进度并暂停。
13. 较新的 EPUB 与较旧的 PDF 同时存在时选择较新的 EPUB；同版本则优先 PDF。
14. 文件平铺保存在指定目录，名称字段顺序符合“系列名—作者—书名—卷次—蓝思—AR”，且不包含出版社和 ISBN。
15. 额度耗尽后未完成项目仍保留，记录下一次尝试时间；常驻 worker 在 24 小时后自动续跑。
16. 程序重启后可以查看、暂停、继续、取消、删除或重试队列项目，删除项目不会删除已下载文件。

## 10. 风险与依赖

| 风险/依赖 | 影响 | 应对方案 |
|---|---|---|
| 渠道服务条款不允许自动化 | 功能无法合规上线 | 开发前确认条款；必要时改为用户确认的辅助模式 |
| 网站域名或页面结构频繁变化 | 自动化容易失效 | 渠道适配层解耦、错误检测、版本化选择器 |
| 验证码、下载额度或限流 | 批量任务被中断 | 合规暂停并要求用户处理，不绕过限制 |
| 同名书、系列书、不同卷次 | 误下载 | 作者/ISBN/卷次交叉验证，冲突时强制确认 |
| “最新版本”元数据不完整 | 版本选择不可靠 | 将不确定性展示给用户，不以上传时间代替版次 |
| AR/蓝思正式数据可能受限或缺失 | 无法可靠写入 | 优先用户数据；估算值明确标记；允许省略 |
| PDF 可能为低质扫描或文件损坏 | 阅读体验差 | 文件头、页数、大小及扫描质量基础检查 |
| 文件重命名误删有效信息 | 元数据损失 | 精确后缀规则、预览、保留映射和可恢复机制 |

## 11. 第一轮 Review 已确认的产品决策

1. 首版采用“命令行 + 简单确认界面”。
2. 高置信度候选默认自动下载；冲突或低置信度候选仍需用户确认。
3. 版本新旧优先于格式：较新的 EPUB 优先于较旧的 PDF；同版本优先 PDF。
4. “最新版本”不包括正文未变化的普通重印版。
5. 文件名不包含出版社和 ISBN；包含系列名与卷次，字段顺序为“系列名—作者—书名—卷次—蓝思—AR”。
6. AR/蓝思值缺失时允许查询外部数据源并重新估算；估算值也写入文件名，但必须带 `~` 标记。
7. 不自动建立子目录，所有图书平铺保存到用户指定文件夹。
8. 首版查重只检查用户指定的保存目录和本工具历史记录，不扫描其他电子书库。
9. 首版只支持网页，不支持 PC 客户端。
10. 登录以外的网页操作默认使用无头浏览器；网站地址失效时由用户提供新地址。
11. 高置信度结果可自动下载，但仍须遵守用户账号实际权限与下载额度，不绕过验证码、额度、DRM 或其他访问限制。
12. 下载列表持久化保存，并提供查看、暂停、继续、取消、删除和失败重试功能。
13. 当期额度用完后，保留剩余任务并记录 24 小时后的续跑时间；常驻 worker 届时自动继续，登录失效则等待用户重新登录。

## 12. 示例输出

假设原文件名为：

`The Explorer (Katherine Rundell) (z-library.sk, 1lib.sk, z-lib.sk).pdf`

书单提供正式数据时，建议重命名为：

`Katherine Rundell - The Explorer - [Lexile 740L] - [AR 5.4].pdf`

书单数据明确标为估算时，建议重命名为：

`Crookhaven - J.J. Arcanjo - The School for Thieves - Book 1 - [Lexile ~750L] - [AR ~5.1].pdf`

如果作者或难度值无法可靠确定，则宁可省略，不把不确定信息写成确定事实。
